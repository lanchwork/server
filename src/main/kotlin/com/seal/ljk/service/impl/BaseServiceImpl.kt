/*
package com.seal.ljk.service.impl

import com.github.pagehelper.Page
import com.seal.ljk.dao.BaseMapper
import com.seal.ljk.model.Base
import com.seal.ljk.service.IBaseService
import org.springframework.beans.factory.annotation.Autowired

*/
/**
 * Created by chenjh on 2018/12/24.
 *//*

open class BaseServiceImpl<M : BaseMapper<T>, T : Base> : IBaseService<T> {
    
    
    @Autowired
    lateinit var mapper: M
    
    override fun get(id: String): T {
        return mapper.get(id)
    }
    
    override fun getAll(data: T): Page<T> {
        return mapper.getAll(data)
    }
    
    override fun insert(data: T) {
        mapper.insert(data)
    }
    
    override fun update(data: T) {
        mapper.update(data)
    }
    
    override fun delete(id: String) {
        mapper.delete(id)
    }
    
}
*/
